import React from "react";

const GetPhotos = ({ placeID }) => {
  return (
    <div>
      <p>{placeID}</p>
    </div>
  );
};

export default GetPhotos;

// const testReceive = (object1) =>
// {onClick={testReceive(object1)}>test receive
//     console.log(object1);
// }
